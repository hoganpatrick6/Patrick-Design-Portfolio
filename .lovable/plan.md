# Rebuild the Grocery Fresh project page

## Scope
Rebuild only the **Grocery Fresh** case study to follow the supplied reference: a compact editorial introduction, large food photography, centered narrative, a two-column image grid, and a wide campaign strip. Other project pages keep their current structure.

## Layout
- Replace the current generic project arrangement with a dedicated Grocery Fresh sequence.
- Lead with the large “Grocery Fresh” title, small Client and Role details, and the existing project summary in a precise editorial grid.
- Follow with a wide carrot photograph, a centered “Global Image Library” story, a four-image produce grid, and a horizontally cropped campaign artwork strip.
- Keep generous white space, thin typography, square image frames, and responsive single-column behavior on smaller screens.

## Editing
- Keep each major section as its own selectable canvas block so it can still be moved and resized.
- Keep every image replaceable and preserve individual text styling controls.
- Restore the project headline as a permanent built-in block even if an older saved edit marked it hidden.

## Visual assets
- Create a cohesive Grocery Fresh image set inspired by the content and composition of the reference, without embedding or copying the reference screenshot itself.
- Use clear descriptive alt text for every image.

## Technical details
- Add Grocery Fresh-specific asset imports and page content while preserving the shared layout for all other projects.
- Add default canvas positions sized to the existing 12-column, 24px baseline grid.
- Verify the page at desktop and mobile sizes, including editor selection and replacement controls.
